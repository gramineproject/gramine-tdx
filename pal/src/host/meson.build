subdir('linux-common')
if skeleton
    subdir('skeleton')
endif
if direct
    subdir('linux')
endif
if sgx
    subdir('linux-sgx')
endif

if vm or tdx
    subdir('vm-common')
endif
if vm
    subdir('vm')
endif
if tdx
    subdir('tdx')
endif
